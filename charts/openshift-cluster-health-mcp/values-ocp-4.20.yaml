# OpenShift 4.20 values overlay
# Usage: helm install mcp-server ./charts/openshift-cluster-health-mcp -f values-ocp-4.20.yaml

image:
  tag: "ocp-4.20-latest"

openshiftVersion:
  target: "4.20"
  kubernetes: "1.33"
